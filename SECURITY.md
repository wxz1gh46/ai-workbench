# 安全政策

## 报告漏洞

如果你发现了安全漏洞，**请不要公开提交 Issue**。

请通过以下方式私下报告：

1. 使用 GitHub 的 [Private vulnerability reporting](https://github.com/wxz1gh46/ai-workbench/security/advisories/new)，或
2. 通过 GitHub 私信联系维护者 `@wxz1gh46`

请在报告中尽量包含：

- 漏洞类型与影响范围
- 复现步骤（最小可复现示例）
- 受影响的版本或提交
- 可能的修复建议（可选）

我们会在 **72 小时内**确认收到，并在修复后于 `CHANGELOG.md` 中致谢（除非你希望匿名）。

## 支持范围

| 版本 | 支持状态 |
| --- | --- |
| 最新主分支 | ✅ 接受修复 |
| 历史提交 | ❌ 不再维护 |

## 设计层面的安全约定

本项目在设计上遵循以下不可绕过的约束，它们有单元测试覆盖：

1. **密钥零硬编码**：所有凭据只从环境变量或 OS Keychain 读取；源码、日志、数据库、审计记录、接口响应中均不出现凭据明文。
2. **本地加密**：需要持久化的敏感配置使用 AES-256-GCM（带认证标签）加密，口令用 scrypt 派生。
3. **危险操作二次确认**：部署、删除、安装插件、付费数据调用等危险动作在服务端强制校验 `confirm === true`，未确认返回 `428 CONFIRM_REQUIRED`。
4. **路径穿越防护**：文件与 Office 工具被限制在 `workspace.rootPath` 内，拒绝 `../` 等越界访问。
5. **合规红线**：项目不实现、也不接受绕过反爬、验证码、共享账号或破解授权的插件与能力。
6. **审计留痕**：所有外部服务调用都会写入分域审计日志，`detail` 字段自动脱敏。

## 已知的安全边界

- 本项目的**联网检索、付费数据源、部署平台、推送渠道**全部需要用户自行配置凭据，工作台不代持任何第三方账号。
- 本地预览服务默认只监听 `127.0.0.1`，不会暴露到局域网。
- 若你不配置 `WORKBENCH_SECRET_KEY`，系统会在本机 `data/secrets/local.key` 生成一把，**跨机迁移会导致旧密文无法解密**。
